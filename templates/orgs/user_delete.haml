-extends "includes/modax.html"
-load i18n

-block fields
  -if owned_orgs
    -trans "Deleting this user will also delete the following workspaces owned by this user."

    .mt-2.mb-2.ml-8
      %ul.list-disc
        -for org in owned_orgs
          %li
            {{ org.name }}

  -blocktrans trimmed with username=object.username
    Are you sure you want to delete <b>{{ username }}</b>?"
