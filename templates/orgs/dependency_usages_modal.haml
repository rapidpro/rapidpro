-extends "includes/modal.html"
-load smartmin i18n

-block modal
  -block pjax
    -if dependents
      -for dep_type in dependents
        -include "includes/dependents.haml" with dependents=dep_type
    -else
      -trans "None"
