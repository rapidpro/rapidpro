-extends 'includes/modax.html'
-load i18n smartmin

-block fields

  .message.mb-4
    -blocktrans trimmed
      Any tickets opened within the selected range will be included.

  .flex
    .start.flex-grow
      -render_field 'start_date'
    .px-1
    .end.flex-grow
      -render_field 'end_date'

  -render_field 'with_fields'
