<temba-compose
  name="{{ widget.name }}"
  {% include "django/forms/widgets/attrs.html" %}
  value="{% if widget.value %}{{widget.value}}{%endif%}"

  {% comment %} todo - infinite loop troubleshooting
  currentText="{{ widget.text }}"
  currentAttachments="{{ widget.attachments }}" {% endcomment %}
>
</temba-compose>