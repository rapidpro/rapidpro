server {
    listen 8000 default;
    server_name *.push.al *.ilhasoft.mobi *.ilhasoft.dev;

    # location = /favicon.ico { access_log off; log_not_found off; }
    location /sitestatic/ {
        root $PROJECT_PATH;
    }

    location / {
        include /etc/nginx/proxy_params;
        proxy_pass http://unix:${PROJECT_CONF}/gunicorn.sock;
    }
}
